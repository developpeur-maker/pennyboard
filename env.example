# Variables d'environnement pour PennyBoard
# Copiez ce fichier vers .env.local et remplissez les valeurs

# Clé API Pennylane (OBLIGATOIRE)
VITE_PENNYLANE_API_KEY=your_pennylane_api_key_here

# URL de base de l'API Pennylane (optionnel)
VITE_PENNYLANE_BASE_URL=https://app.pennylane.com/api/external/v1

# Nom de l'entreprise (optionnel)
VITE_COMPANY_NAME=DIMO DIAGNOSTIC

# ID de l'entreprise (optionnel)
VITE_COMPANY_ID=dimo-diagnostic
